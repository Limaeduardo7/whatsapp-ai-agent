### Memórias Registradas em 17 de fevereiro de 2026

- **Atualizações**:  
  - A integração com o Trello não foi bem-sucedida devido a problemas de autenticação com a API.  
  - A skill Trello foi instalada, mas apresentou vulnerabilidades.  
  - A integração com o Notion foi iniciada com a chave fornecida.  
- **Desafios**:  
  - Inconsistência nas atualizações do kanban.  
  - Chamadas de API para marcar tarefas concluídas retornando "Not Found".  
  - Falhas na autenticação do bot WhatsApp, mas melhorias foram implementadas.
## Durable decisions (chat)
- Notion is now the primary operational backend for task management in a new workspace (previous workspace had limitations).
- Tasks board was reset and repopulated with agent-assigned tasks using an `Agent` property (Jarvis, Estaleiro, Escrivão, Inspetor, Researcher, Designer, Analyst, Growth, SEO, Social).
- Rule set by Eduardo: whenever agents produce documents, they must write/register them in the Notion board "Document Hub".
- Strategic change requested: stop using Notion for CRM; migrate CRM database to the Notion CRM board and continue updates there.

## Durable updates (late session)
- Eduardo reinforced WhatsApp behavior: agent must send only one short greeting at conversation start; no repeated greetings.
- All agent `.md` files under `/root/.openclaw/agents/*/agent/*.md` were uploaded into Notion Document Hub (16 docs total).
- CRM Notion database (`Clients`) was cleaned (archived all rows), then restored to continue migration/work (10 active clients restored).
- Attempt to switch model to `openai-codex/gpt-5.3-codex-spark` failed because the model is not allowed in current environment.

## Durable updates (pre-compaction flush)
- Eduardo deu autonomia explícita para organizar o Notion quando necessário.
- Tentativas de trocar para o modelo `openai-codex/gpt-5.3-codex-spark` falharam; modelo não permitido no ambiente atual.
- Pedido de atualizar OpenClaw para última versão foi executado, mas update ficou `skipped` por `not-git-install` (permaneceu em 2026.2.9).
- Agente WhatsApp comercial foi ajustado para: saudação única curta, 1 mensagem por turno (sem rajada), e bloqueio de preço antes de qualificar escopo.
- Após feedback de Eduardo, saudação foi refinada para: "Oi! Sou o Eduardo. Me conta rapidamente: você precisa de landing page, site institucional ou loja virtual?"
- Tarefa no board referente à "Análise de Gargalos WhatsApp" foi corrigida manualmente e marcada como `done` (id `t1771076100050`) após falha automática com "Not Found".
- Eduardo pediu para migrar todos os agentes que estiverem em `gpt-4o`/`gpt-4o-mini` para Kimi; tentativa imediata foi bloqueada porque `moonshot/kimi-k2.5` não está permitido/configurado na instância atual.
