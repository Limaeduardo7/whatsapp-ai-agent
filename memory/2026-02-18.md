# 2026-02-18 — Log de Atividades

## 10:47 BRT — Feedback: WhatsApp Preços
- **Problema:** Agente WhatsApp enviando apenas preço do site multipage quando lead pede valores
- **Deveria:** Enviar todos os preços (multipage + landing + landing Google + implantação + manutenção + copy liberada + boost)
- **Ação:** Crustáceo escalonou para Jarvis corrigir script do agente

## 10:49 BRT — Atualização: Domínio
- **Ajuste:** Não informar que domínio está incluso
- **Oferecer:** Apenas hospedagem gratuita
- **Deixar claro:** Domínio é por conta do cliente

## 11:03 BRT — Correção Concluída
- **Script atualizado:** evo_auto_reply.py com preços completos
- **Serviço reiniciado:** evolution-bridge operacional
- **Status:** ✅ WhatsApp Comercial corrigido

## 19:39 BRT — Correção crítica Kanban/API (Not Found)
- **Problema:** atualização de status de tarefas para `done` falhava com erro **Not Found** na API.
- **Ações aplicadas:**
  - Adicionados endpoints de atualização por ID no backend ativo (`scripts/evo_auto_reply.py`):
    - `PATCH /api/tasks/{id}`
    - `PUT /api/tasks/{id}`
    - `POST /api/tasks/{id}/complete`
  - Mantida sincronização com `agents_config.json` + SQLite (`tasks`) ao atualizar status.
  - Serviço da API reiniciado (`uvicorn scripts.evo_auto_reply:app --port 8001`).
- **Validação:** testes locais retornaram `200 OK` para patch e complete em tarefa real.
- **Resultado durável:** fluxo de marcar tarefa como concluída por ID está operacional e reduz recorrência do erro de integração do Analyst.

## 19:56 BRT — Diretriz do Eduardo
- **Decisão:** não utilizar mais nenhuma ferramenta do Notion.
- **Ação executada:** serviços ligados ao Notion interrompidos no host.
- **Regra atualizada (19/02/2026):** Notion deve permanecer ATIVO por padrão. Só desativar com ordem explícita do Eduardo.
