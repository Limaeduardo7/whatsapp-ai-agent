- Mensagem enviada alertando que a saudação inicial do agente do WhatsApp deve ser apenas uma saudação, sem informações adicionais, aguardando a resposta do lead. Correção necessária devido a erro atual.
- **Bot WhatsApp corrigido:** Prompt em `/root/clawd/scripts/evo_auto_reply.py` editado para não enviar preços na saudação. Regra 29 adicionada: primeira mensagem = apenas saudação curta.
- **Modelo trocado para Kimi K2.5 (NVIDIA):** `MODEL_ID` alterado para `moonshotai/kimi-k2-instruct`, endpoint para `integrate.api.nvidia.com`. **PENDENTE: NVIDIA API Key não configurada — precisa do Eduardo.**
- **Bridge reiniciado** com sucesso via `systemctl restart evolution-bridge.service`.
- **Arquitetura do bot WhatsApp:** Bridge Python/FastAPI em `/root/clawd/scripts/evo_auto_reply.py`, porta 8001, service unit `evolution-bridge.service`. Doc completa em `/root/clawd/WHATSAPP_COMERCIAL_AGENT_CONFIG.md`.
- Eduardo pediu para trocar API da OpenAI para Kimi K2.5 da NVIDIA em todos os agentes. Bot WhatsApp foi o primeiro corrigido. Falta aplicar nos demais agentes (cron jobs usam `google-antigravity/gemini-3-pro-high` como subagent model).
- **NVIDIA API Key recebida do Eduardo:** `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` — configurada no bot WhatsApp.
- **Modelo final do bot WhatsApp:** `moonshotai/kimi-k2-instruct` via `integrate.api.nvidia.com/v1/chat/completions`. API testada e funcionando.
- **Prompt atualizado:** Regras 29-32 adicionadas — saudação sem preço, ser direto, destacar benefícios (hospedagem gratuita, suporte técnico ilimitado), mostrar valor antes do custo.
- **Números protegidos (PROTECTED_NUMBERS):** `5554913917178` e `5554991879262` — nunca podem ser bloqueados.
- **Descoberta crítica:** Eduardo não consegue testar o bot pelo próprio WhatsApp porque seu número (`5554991879262`) é o OWNER_NUMBER conectado ao Evolution. Mensagens dele chegam como `fromMe=true` e são ignoradas. Bot só responde a leads externos. Eduardo precisa testar de outro número ou ajustar o código.
- **ignored_numbers.json restaurado:** contém apenas `["5554913917178"]` após Eduardo pedir para ativar lista de bloqueio.
- **Vários agentes (SEO, Social, Inspetor, Escrivão, Growth) reportaram erros ao marcar tarefas como "done" via API — endpoint `/api/tasks/mark` e `/api/tasks/done` retornam 404.** Precisa investigar.