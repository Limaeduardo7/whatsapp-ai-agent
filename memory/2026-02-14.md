# 2026-02-14

## WhatsApp Bot Fix (18:00-21:30 BRT)
- **Causa raiz:** Todas as cotas de IA esgotadas simultaneamente:
  - `google-antigravity/gemini-3-pro-high`: cooldown, quota resets ~145h
  - `google-antigravity/gemini-3-flash`: cooldown, quota resets ~100h
  - `anthropic/claude-sonnet-4-20250514`: rate_limit_error (429)
  - Gateway retornava "All models failed (3)" para qualquer request via `/v1/chat/completions`
- **Solução:** Trocou backend do bot (`evo_auto_reply.py`) de Gateway local → OpenAI API direta (`gpt-4o-mini`)
  - API key OpenAI encontrada em `.bashrc`: `OPENAI_API_KEY`
  - Também encontrada `GEMINI_API_KEY` mas Google API free tier também esgotado (429)
- **Melhorias aplicadas:**
  - Fallback amigável quando modelo retorna erro (antes ficava mudo)
  - Log de erros do Gateway (`else` branch no status != 200)
- **Teste:** mensagem simulada processada com sucesso (OpenAI 200 OK → sendText 201 Created)
- **Config OpenClaw:** Adicionados profiles `openai:default` e `google:default` com API keys em `openclaw.json`

## Notion GUI - Charts
- Gráficos (AreaChart, PieChart, BarChart) adicionados ao CRM tab usando recharts 3.7.0
- Deploy feito no Netlify (`notion-eduardo-777`)
